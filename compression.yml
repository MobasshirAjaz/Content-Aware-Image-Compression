name: image_compression
channels:
  - pytorch
  - defaults
dependencies:
  - python=3.10
  - pytorch
  - torchvision
  - cpuonly
  - pip
  - pip:
      - opencv-python
      - numpy
      - pillow
      - supervision
      - transformers
      - ollama
      - git+https://github.com/facebookresearch/sam2.git
